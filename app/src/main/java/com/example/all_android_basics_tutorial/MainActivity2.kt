package com.example.all_android_basics_tutorial

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Button
import android.widget.TextView

class MainActivity2 : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main2)
//        val name = intent.getStringExtra("EXTRA_NAME")
//        val age = intent.getIntExtra("EXTRA_AGE" , 0)
//        val nameText = "${name} " + "Hey this is my name"
//        val ageText = "My age is  " + "${age}"
//        val displayName = findViewById<TextView>(R.id.nameTextId)
//        displayName.text = nameText
//        val displayAge = findViewById<TextView>(R.id.ageTextId)
//        displayAge.text = ageText
    }
}